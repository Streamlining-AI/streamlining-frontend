export interface FormUpload {
  name: string;
  github_url: string;
  type : string;
  user_id : string;
  is_visible : boolean;
  description : string;
  github_code: string;
  model_version : string;
  banner_url : any; // File then Str
}
